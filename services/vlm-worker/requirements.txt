# CONFIT self-hosted VLM worker (Qwen2.5-VL-7B-Instruct, Apache-2.0).
# GPU runtime — installed in the Modal worker image ONLY (never in the Vercel
# backend function, which stays lightweight).
torch>=2.4.0
torchvision>=0.19.0
# Qwen2.5-VL (Qwen2_5_VLForConditionalGeneration) requires a recent transformers.
transformers>=4.50.0
accelerate>=0.33.0
# 4-bit NF4 quantization for the served worker (cold-start + VRAM reduction).
bitsandbytes>=0.43.0
qwen-vl-utils>=0.0.8
huggingface_hub>=0.24.0
Pillow>=10.4.0
numpy>=1.26.0
httpx>=0.27.2
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0
